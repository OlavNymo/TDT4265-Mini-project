# RGB dataset-specific training configuration
# Inherits from base_config.yaml

# Dataset configuration
dataset:
  name: "rgb"
  train_path: "/datasets/tdt4265/ad/open/Poles/rgb/images/train"
  val_path: "/datasets/tdt4265/ad/open/Poles/rgb/images/valid"
  test_path: "/datasets/tdt4265/ad/open/Poles/rgb/images/test"
  label_path: "/datasets/tdt4265/ad/open/Poles/rgb/labels"
  classes: ["pole"]
  num_classes: 1

  # Data preparation settings
  cache_images: true
  single_cls: true # Single class detection

# Model customization for RGB data
model:
  name: "yolov12n" # Light model suitable for edge deployment
  pretrained: true
  input_size: 640

  # RGB-specific model tuning
  channels: 3 # Standard RGB channels

# Training adjustments for RGB data
training:
  batch_size: 16
  epochs: 100
  learning_rate: 0.001

  # Class weights (if needed for imbalance)
  class_weights: [1.0]


  # Geometry augmentations
  scale: 0.5
  translate: 0.1
  fliplr: 0.5

# Evaluation specifications
evaluation:
  # Focus metrics for RGB evaluation
  metrics: ["precision", "recall", "mAP50", "mAP50-95"]

  # Visualization settings
  visualize_predictions: true
  max_visualizations: 100
